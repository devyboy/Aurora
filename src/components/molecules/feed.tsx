const Feed = () => {
  return (
    <div className="flex flex-col">
      {/* {data.map((data) => (
        <PostItem key={data.post.id} data={data} />
      ))} */}
    </div>
  );
};

export default Feed;
